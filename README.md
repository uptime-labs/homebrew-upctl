# Homebrew tap for upctl

This directory contains brew tap for the cli

## Installation Options

### Homebrew (macOS and Linux)

```bash
# Add the tap
brew tap uptime-labs/upctl

# Install upctl
brew install upctl
```
